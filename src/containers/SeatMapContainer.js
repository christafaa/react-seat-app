import React, { Component } from 'react';
import { connect } from 'react-redux';

class SeatMap extends Component {

  render() {
    return (
      <React.Fragment>

      </React.Fragment>
    )
  }
}

const mapStateToProps = state => {
  return {

  }
}

export default connect()(SeatMap)
